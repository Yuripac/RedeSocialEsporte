<p>
  <strong>Name:</strong>
  <%= @group.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @group.description %>
</p>
<p>
  <strong>Sport:</strong>
  <%= @group.sport.name if @group.sport %>
</p>

<% if current_user == @group.user %>
  <%= link_to 'Edit', edit_group_path(@group) %> |
<% end %>
<%= link_to 'Back', groups_path %>
